[project]
name = "hotels-agent"
version = "1.0.0"
description = "Servidor A2A de busqueda de hoteles (Trip Planner Workshop)"
requires-python = ">=3.10"
dependencies = [
    "strands-agents[a2a]>=0.1.0",
    "bedrock-agentcore>=0.1.0",
]

[tool.agentcore]
entrypoint = "main.py"
